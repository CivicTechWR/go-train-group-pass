{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@designcomputer/supabase_mcp_server@1.1.0"],
      "env": {
        "NEXT_PUBLIC_SUPABASE_URL": "${NEXT_PUBLIC_SUPABASE_URL}",
        "SUPABASE_URL": "${NEXT_PUBLIC_SUPABASE_URL}",
        "NEXT_PUBLIC_SUPABASE_ANON_KEY": "${NEXT_PUBLIC_SUPABASE_ANON_KEY}",
        "SUPABASE_SERVICE_ROLE_KEY": "${SUPABASE_SERVICE_ROLE_KEY}"
      },
      "description": "Direct access to Supabase database, auth, and storage operations"
    },
    "postgres": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres@1.0.1", "${POSTGRES_CONNECTION_STRING}"],
      "description": "Advanced PostgreSQL queries and database optimization"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@1.0.0", "/Users/andrelevesque/Projects/go-transit-group"],
      "description": "Fast file operations and directory management"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking@1.0.0"],
      "description": "Complex problem solving for group formation algorithm design"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@1.0.0"],
      "description": "Maintain context across sessions, store architectural decisions"
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch@1.0.0"],
      "description": "Test API endpoints and fetch GO Transit GTFS feeds"
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search@1.0.0"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Research Next.js 15, Supabase, and tRPC best practices"
    }
  }
}
